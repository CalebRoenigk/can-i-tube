{
  "name": "can-i-tube",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:CalebRoenigk/can-i-tube.git",
  "author": "Chris Weissenberger <chris@Chris-Weissenberger.local>",
  "license": "MIT",
  "browserslist": [
    "last 1 Chrome versions"
  ],
  "dependencies": {
    "gh-pages": "^3.1.0",
    "jquery": "^3.5.1",
    "moment": "^2.27.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "scripts": {
    "dev": "parcel index.html --out-dir docs",
    "build": "parcel build index.html --out-dir docs --public-url ./",
    "predeploy": "rm -rf dist && rm -rf docs && parcel build index.html --out-dir docs --public-url ./",
    "deploy": "gh-pages -d docs"
  }
}
